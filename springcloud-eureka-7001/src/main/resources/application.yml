server:
  port: 7001

#Eureka配置
eureka:
  instance:
    hostname: eureka7001    #Eureka服务端的实例名称
  client:
    register-with-eureka: false   #表示是否向Eureka注册中心注册自己（写的就是服务器，自然不用注册）
    fetch-registry: false         #false:表示自己是注册中心
    service-url:
      defaultZone: http://eureka7002:7002/eureka/
